# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

---

## 安全ルール（最優先）

### 削除系コマンドの禁止（重要）

以下のルールはこのワークスペース内のすべての会話で絶対に守られる：

- Claude はファイルまたはディレクトリを削除するコマンドを一切生成してはならない。
  例：rm, rm -rf, rm *, rmdir, unlink, cache --delete,
      lftp mirror --delete, rsync --delete, git clean -df, find -delete 等。

- 削除が必要な場合でも、Claude は削除コマンドを提案せず、
  「手動で削除してください」といった説明に留めること。

- 削除の推奨・削除操作の自動判断も禁止。

- ssh / lftp / デプロイ系スクリプトを生成する場合でも、
  削除コマンドの生成は禁止。

これらはすべての会話・コード生成に適用される。

不要になったファイルは削除せず `DELETE/` へ移動する。

### シークレット管理（重要）

- `config/master.key` など機密ファイルを `git add` するコードを生成してはならない
- デプロイスクリプト・セットアップ手順でも同様
- シークレットは必ず環境変数（RAILS_MASTER_KEY 等）で渡すこと
- `.gitignore` への追加を確認する手順を必ずコードに含めること
- 初回コミット前に `git status` でステージング確認を促すこと

---

## 事業前提

本事業は **B2B** である。成果物はビジネス利用者に向けたものとして設計・執筆する。

## コンテンツの表記（最重要）

**コンテンツは絶対にですます調で書く。だである調で書くことを禁止する。**

ここでいうコンテンツとは、成果物として利用者の目に触れる文章を指す。

| 対象 | 例 |
|---|---|
| 画面表示テキスト | UI のラベル・見出し・説明文・エラーメッセージ |
| 動画の画面文言とナレーション | 字幕・読み上げ原稿 |
| 利用者向けドキュメント | 使い方・案内・記事 |

適用外は、開発者だけが読む内部文書（`CLAUDE.md`、`requirements.md`、`SPEC/`、`DOCS/`、`WORK/` 等）とコード・コメント・コミットメッセージである。

- 開発は **日本語版のみ**とする。多言語対応を行わない。
- 文章の執筆は `writer` agent に行わせる。プロのライティングとして成立する品質を要求する。

## プロジェクト概要

動画仕様書（Markdown / プレーンテキスト）を入力として mp4 を生成する **Agent Skill** を作るリポジトリ。Web アプリケーションでもライブラリでもない。

| 項目 | 値 |
|---|---|
| 成果物 | `SKILL.md` 単体（デモ版は `scripts` / `references` / `assets` を持たない） |
| スキル名 | `spec-to-mp4-video` |
| 出力 | mp4（1920x1080 / 30fps / H.264 / 無音） |
| 対象ツール | Claude Code |

- 仕様の Single Source of Truth は `requirements.md`。仕様に関する判断は必ずここを読んでから行い、推測で補完しない。
- リポジトリ本体を SSOT とし、ツールのスキルディレクトリへは symlink で設置する。
- `requirements.md` 第16節に「仕様書に書くこと／成果物 SKILL.md に書くこと」の境界が定義されている。SKILL.md を書く際は必ずこの境界に従う。

## 前提

- 時刻はすべて **JST**（`Asia/Tokyo`）。日時を書くときは `2026-08-08 15:04 JST` のようにタイムゾーンを明示する。
- ファイルのエンコードはすべて **UTF-8**（BOM なし）、改行は LF。
- 内部文書・コメント・PR 本文は日本語で書く。コード識別子と技術用語は原語のまま。

## 開発フロー（TDD 厳守）

`plan` → `red test` → `coding` → `green test` の順を厳守する。順序の入れ替えと省略を行わない。

1. **plan** — 変更範囲と受け入れ条件を先に書く
2. **red test** — 失敗するテストを先に書き、**実際に失敗することを実行して確認する**
3. **coding** — テストを通す最小の実装を書く
4. **green test** — テストが通ることを実行して確認する

テストの種類とフレームワークは `DOCS/TM.md` の対応表から選ぶ（JavaScript は Jest、Ruby は RSpec、E2E は Playwright 等）。実行コマンドは選定時にこの節へ追記する。

### テストの配置と対象

- テストは **`test/pr<PR番号>/`** に作成する。
- **テストの対象は開発サーバーとする。** 本番環境に対して実行しない。接続先は環境変数で受け取り、値をテストコードへ直接書かない。
- **すべての PR を対象**として、PR 本文に書かれたユーザーテスト手順の実行スクリプトを作成する。手順の番号とスクリプトのステップを1対1で対応させ、要約・省略・順序変更をしない。

## 技術スタックの既定

新規に構築する場合の既定を次のとおりとする。既定から外れる場合は理由を明示する。

| 層 | 既定 |
|---|---|
| フロントエンド | Next.js |
| バックエンド | Ruby on Rails |
| データベース | PostgreSQL |

用途に応じて次の API を追加してよい。

| 用途 | 採用してよいもの |
|---|---|
| AI・解析・画像加工 | FastAPI |
| 高速並列処理・リアルタイム通信 | Gin |

## アーキテクチャ

- 規模に応じて **マイクロサービスアーキテクチャ**、**MVC アーキテクチャ**、**API Gateway**、**メッセージング**を意識して設計する。小さな規模で過剰に分割しない。
- **車輪の再発明を避ける。** メンテナンスコストとセキュリティの観点から、安全なライブラリ・フレームワーク・OSS・SaaS を適用し、オリジナルコードを少なく保つ。
- 採用するライブラリは、保守が継続されていること・既知の脆弱性がないことを確認してから使う。

## デプロイ

| 対象 | デプロイ先 |
|---|---|
| フロントエンド | Vercel（無料プラン） |
| バックエンド・管理画面 | Railway（または Render）（無料プラン） |

- ドメインは原則 **`rictaworks.jp` のサブドメイン**とする。
- **バックエンドのドメインは隠蔽する。** 利用者から到達可能な形で露出しない。
- Web の場合、デプロイは**ヘッドレスで実行する**。
- **CD はこのリポジトリで扱わない。** Claude Desktop 側で設定する。

### 作業の引き継ぎ点

次の工程から先は Claude Desktop で作業する。この工程を Claude Code 側で行わない。

| 種別 | 引き継ぎ点 |
|---|---|
| Web | デプロイから先 |
| デスクトップ・スマートフォン | ビルドから先 |
| ESP32 | 焼き込みから先 |

## 認証

- 認証は **Google ログイン**とする。
- **一般消費者が実際に使える手段でログインできること。** 開発者向けの近道（デバッグ用ログイン、固定トークン、URL パラメータでの認証迂回等）を本番の UI に露出しない。
- **環境の判定を必ず実装して分岐できるようにする。** テストを可能にするため、開発環境は認証済みに分岐する。この分岐が本番環境で有効にならないことをテストで担保する。

## ブランチ運用

- **main ブランチでの作業を禁止する。** 変更前に必ずブランチを切る。
- `src/*` **以外**の変更（ドキュメント・設定等）は main への push を許可する。
- `src/*` の変更は **必ず PR を作成する**。main への直接 push を行わない。

## PR 規約

PR 本文に **非エンジニア向けのユーザーテスト手順**を丁寧に書く。専門用語を使わず、次を必ず含める。

- 何を確認するのか（利用者から見て何が変わるのか）
- 手順（クリック・入力・実行を1ステップずつ、省略せずに）
- 期待する結果（どうなれば合格か）
- うまくいかなかったときの報告方法

テンプレート: `.github/pull_request_template.md`

PR を作成したら、`pr-checker` agent でタイトルと本文を日本語に整え、`tester` agent でユーザーテスト手順の実行スクリプトを `test/pr<PR番号>/` に作成する。

## CI/CD

- **CI は必須。** `.github/workflows/ci.yml` を通らない変更をマージしない。
- CD の扱いは「デプロイ」の節に従う。

## コミット前チェック

commit 前に必ず次を実施する。

1. `/security-review` を実行し、`.claude/OWASP10.md` の観点で確認する
2. `git status` でシークレットファイルがステージングされていないことを確認する
3. `.gitignore` に機密ファイル（`.env` 等）が含まれていることを確認する

## コーディング規約

以下は Claude の既定挙動と反するため、例外なく適用する。

- **フォールバック禁止。** 値が取得できない・処理が失敗した場合に既定値で握りつぶさない。例外を送出し、呼び出し元で明示的に処理する。`catch` して `null` や空配列を返す実装を書かない。
- **例外処理を明示的に書く。** 想定される失敗ごとに、原因が特定できるメッセージを持つ例外を投げる。
- **デバッグトレース可能にする。** 処理の入口・出口・分岐で、入力値と判断根拠をログに残す。障害時にログだけで実行経路を再構成できる粒度にする。
- **制御構文・条件構文以外は関数またはクラスに置く。** トップレベルに処理を直接書かない。
- **グローバル変数を禁止する（セキュリティ上の理由）。** 状態は引数・戻り値・インスタンスで受け渡す。
- **文字列リテラルをファイルまたはデータベースに分離する。** 画面文言・メッセージをコードへ直接埋め込まない。**ハードコードを検出するテストを書く。**
  本プロジェクトでは特に重要 — 動画の画面文言は構成データファイルにのみ保持し、シーン実装コードへ直書きしない（`requirements.md` 10.1）。

設計原則は `DOCS/development-principles.md`（YAGNI / KISS / DRY / SOLID）に従う。

## UI / デザイン

- アイコンは **FontAwesome** を使用する。
- **絵文字を使用しない。** コード・UI・内部文書・コミットメッセージのいずれにも書かない。
- **ネイティブの `alert()` / `confirm()` / `prompt()` をプロジェクト全体で使用禁止とする。** ダイアログが必要な場合は UI コンポーネントとして実装する。
- 画像は **AI 生成**で用意する。
- デザイン指定がある場合は `app-ui/` にモックが置かれる。存在する場合はそれに従う。`app-ui/` は参照専用であり、編集スコープに含めない。
- デザイン判断は `DOCS/CRAP.md`（Contrast / Repetition / Alignment / Proximity）に従う。

## 動画成果物の規約

成果物が動画である場合に適用する。

- ナレーションとアニメーションの同期は、**カット単位のナレーション**とする。文単位・秒単位での同期を行わない。
- デフォルトの voice は **四国めたん（ノーマル）** とする。
- ナレーション原稿はコンテンツであるため、ですます調で書く。

本リポジトリのデモ版は `requirements.md` 8.1 により音声を持たない。ナレーションが仕様の範囲に入った時点で本節を適用する。

## 環境変数

- 環境変数は `.env` を参照する。
- 値をコード・内部文書・コミットに書かない。記述してよいのは変数名のみ。
- `.env` は `.gitignore` に含める。

## 動作確認手段

フロントエンドの確認は次のいずれかで行う。目視の主張のみで完了としない。

- `curl` — レスポンス・ヘッダ・ステータスコードの確認
- `wget --mirror` — 静的出力全体の取得
- Playwright — 画面操作・スクリーンショット・コンソールログの確認

## ドキュメント運用

| ディレクトリ | 用途 |
|---|---|
| `TASKS/` | タスク |
| `DEBUG/` | バグ報告 |
| `CLIENT/` | クライアント要望等 |
| `WORK/` | 作業報告 |
| `ENV/DEVELOPMENT.md` | 開発環境 |
| `ENV/PRODUCTION.md` | 本番環境 |
| `SPEC/` | 仕様書、リバースエンジニアリング（ER図・DFD・シーケンス図・クラス図・状態遷移図・ユースケース図） |
| `DELETE/` | ゴミ箱。不要になったファイルはここへ移動する |
| `app-ui/` | デザインモック（参照専用） |

- **`README.md` と `SPEC/` に未実装のものを書かない。** 予定・構想・これから導入する手順を書かない。書いてよいのは、実際に存在し動作するものだけとする。
- 図解は **mermaid** で書く。

## 参照ドキュメント

該当する作業に着手する際に読む。

| ファイル | 内容 |
|---|---|
| `requirements.md` | 本スキルの仕様（SSOT） |
| `.claude/QC10.md` | 品質管理10項目 |
| `.claude/OWASP10.md` | OWASP Top 10（security review の観点） |
| `.claude/CC.md` | コンプライアンスチェック10項目 |
| `.claude/Manager.md` | GitHub Issue ベース並列開発の運用 |
| `DOCS/TM.md` | テストメソッドとテストフレームワーク |
| `DOCS/development-principles.md` | 開発原則 |
| `DOCS/CRAP.md` | デザイン4原則 |

## agent 構成

`.claude/agents/` に定義する。規模に応じて使い分ける。

| agent | 役割 |
|---|---|
| `director` | 目的と優先順位の決定、範囲外の切り分け |
| `project-manager` | Issue 分割・依存設計・進行管理（`.claude/Manager.md` に準拠） |
| `designer` | 画面設計・配色・可読性（`DOCS/CRAP.md`、`app-ui/` 参照） |
| `debugger` | 不具合の再現・原因特定（`DEBUG/` に記録） |
| `tester` | テスト設計と実行。TDD の red / green を担保し、PR ごとのテストを `test/pr<PR番号>/` に作成する |
| `pr-checker` | 指定 PR のタイトル・本文を日本語に整え、非エンジニア向けユーザーテスト手順を書く。**レビューは行わない** |
| `data-scientist` | 構成データの正規化・尺の算出・集計 |
| `deployer` | スキルの設置（symlink）と環境確認 |
| `writer` | コンテンツの執筆（ですます調）、内部文書の執筆 |
| `service-manager` | 運用ルール・QC10 / CC の適合確認 |
