---
name: spec-to-mp4-video
description: 動画仕様書（シーンの並び・画面に出す文言・想定尺のいずれかを含む文書）を読み取り、1920x1080 / 30fps / H.264 / 無音の mp4 を新規に生成する。仕様書・構成表・絵コンテを渡して動画の書き出しや動画化を求められた場面で使用する。既存の動画・実写素材に対する編集・形式変換・切り出し・要約には使用しない。
---

# 動画仕様書から mp4 を生成する

入力は動画仕様書（Markdown またはプレーンテキストの文書）、出力は mp4 である。

処理を P1 から P9 までの9段で構成する。各段は完了条件を持ち、完了条件を満たさない状態で次段へ進まない。

| 段 | 名称 |
|---|---|
| P1 | 受理判定 |
| P2 | 読み取りと充足判定 |
| P3 | 情報収集 |
| P4 | 台本生成 |
| P5 | 規模判定 |
| P6 | 構成データ生成 |
| P7 | 環境準備 |
| P8 | シーン実装とレンダリング |
| P9 | 表示検証 |

P3 と P4 は、画面に表示する文言が確定していない仕様書を受けたときに働く段である。文言が確定している仕様書では、収集する対象と起こす文言がいずれも存在しないことを確認して通過する。

## 全段に共通する原則

- 不足する情報を推測で補完しない。補完が必要な状況では処理を停止し、利用者に確認する。
- 事実を創作しない。視聴者が入力するコマンド文字列、対象が出力する文言、バージョン番号、尺、シーンの構成は、出所を示せる範囲でのみ扱う。
- 画面に表示する文言そのものは、P4 台本生成でシーンの主張にもとづいて起こす。文言を起こすことと、事実を創作することを区別する。
- 利用者へ提示する文面はですます調で書く。

## P1 受理判定

この手順を適用してよい入力かを確認する。適用する条件は次の3件がすべて成り立つことである。

| 記号 | 条件 |
|---|---|
| C1 | 入力として動画仕様書に相当する文書（シーンの並び・画面に出す文言・想定尺のいずれかを含む文書）が提示または参照されている |
| C2 | 期待される成果物が新規の動画ファイル（mp4）の生成である |
| C3 | 既存の動画・実写素材に対する編集・変換・要約ではない |

次の文脈ではこの手順を適用しない。

| 文脈 | 理由 |
|---|---|
| 動画仕様書そのものの作成・添削の依頼 | 出力が文書であり、C2 が成り立たない |
| 既存 mp4 の形式変換・切り出し・結合 | C3 が成り立たない |
| 既存動画の内容の要約・書き起こし | C2 と C3 が成り立たない |
| 動画生成フレームワークの使い方・仕様の質問 | 出力が説明であり、C2 が成り立たない |
| 実写素材・画面録画の編集依頼 | C3 が成り立たず、この手順は撮影工程を持たない |
| 単一の操作のみで完了する要求 | 手順を適用せず直接処理する領域とする |

C2 が成り立ち C1 が成り立たない場合は、仕様書の所在を1回だけ確認して待つ。このとき仕様書の内容を推測で補完しない。確認への応答で仕様書が提示された時点で C1 が成り立ち、P2 へ進む。仕様書が提示されないまま応答が終わった場合は、この手順を適用しない。

完了条件: C1・C2・C3 がすべて成り立っている。

## P2 読み取りと充足判定

仕様書を読み、必須項目が揃っているかを判定する。必須項目は次の5件である。

| 項目 | 内容 |
|---|---|
| タイトル | 動画の題名 |
| 目的 | 視聴後に視聴者が到達している状態 |
| 想定視聴者 | 前提知識レベル |
| シーン列 | 順序付きのシーン定義（1件以上） |
| 出力先パス | 生成する mp4 の配置先 |

シーン列を構成するシーン定義は、次の3項目を持つ。

| 項目 | 内容 |
|---|---|
| 見出し | シーンの主題 |
| 主張 | そのシーンで視聴者に伝える内容 |
| 想定尺 | 秒数 |

画面に表示する文言そのものと、画面表現の種別は必須項目としない。どちらも設計の段階では確定していないことがあるためである。文言と種別は P4 台本生成で確定する。仕様書に画面表示テキストが書かれている場合は、それを P4 で原文のまま採用する。仕様書に種別が書かれている場合も、それを P4 でそのまま採用する。

任意項目は次の4件である。指定がない場合は既定値を採用し、採用した既定値を完了報告に列挙する。

| 任意項目 | 指定がない場合の既定値 |
|---|---|
| 配色トーン | 暗い背景に明るい文字 |
| フォント指定 | 実行環境で利用できる日本語のゴシック体 |
| シーン間の遷移種別 | 不透明度の変化による切り替え |
| コマンド出力の要点 | 仕様書に記載がある範囲のみを表示する |

必須項目が欠けている場合は、既定値による補完を行わず処理を停止する。欠落した項目を列挙して利用者へ提示し、応答を待つ。事実・尺・シーン構成をこの手順の側で創作しない。想定尺のみが欠けている場合の扱いは P6 の尺の下限に従う。

完了条件: 必須項目がすべて揃っている。

## P3 情報収集

台本に載せる文字列のうち、逐語一致を要するものを集める。逐語一致を要する対象は次の4件である。

| 対象 | 内容 |
|---|---|
| コマンド | 視聴者が入力するコマンド文字列 |
| 依頼文 | 視聴者が対象へ与える指示の文面 |
| バージョン番号 | 画面に表示する製品・ツールのバージョン |
| 出力する文言の要点 | 対象が出力する文言のうち、意味の中核を成す部分 |

これらは、出所を特定できる情報源から集める。情報源として認めるのは、仕様書そのもの、仕様書が参照している文書、実行環境で実際に確認できる出力の3種とする。集めた文字列は、対象・値・出所の3点を対応づけて記録する。

出所を特定できない事実は停止条件とする。値を推測で埋めない。近い値で代用しない。停止したときは、出所を確認できなかった対象を列挙して利用者へ提示し、応答を待つ。

逐語一致を要する文字列が1件も無い仕様書では、該当が無いことを記録してこの段を通過する。逐語一致の対象をこの4件より増やす判断をこの手順の側で行わない。

完了条件: 逐語一致を要すると判断した文字列のすべてに、出所が紐づいている。

## P4 台本生成

シーンの主張と、仕様書に書かれた画面要素の記述から、画面に表示する文言そのものを起こす。起こした文言の一式を台本と呼ぶ。

台本は、シーンごとに次の項目を持つ。

| 項目 | 内容 |
|---|---|
| 見出し | シーンの主題 |
| 種別 | タイトル / 箇条書き / 端末 / コード / 図解 / まとめ のいずれか |
| 画面表示テキスト | 画面に表示する文言そのもの |
| 区分 | 逐語 / 要点 のいずれか |

### 文言の起こし方

区分は文字列ごとに与える。区分が逐語のものは P3 の収集結果をそのまま用い、値を書き換えない。区分が要点のものは、シーンの主張を伝える文言としてこの手順の側で書く。要点として書いてよいのは、見出し・説明文・箇条書きのように、実物との一致を求めない文言に限る。

仕様書に画面表示テキストが確定して書かれている場合は、それを原文のまま採用し、区分を逐語とする。誤字とみられる箇所も含め書き換えない。

### 種別の決定

各シーンの種別を決定する。種別は、その主張をどの画面表現で見せるかの分類であり、次の6件のいずれかとする。

| 種別 | 画面表現 |
|---|---|
| タイトル | 題名と要点を大きく示す画面 |
| 箇条書き | 並列する要点を列挙する画面 |
| 端末 | 入力と出力が行として積み上がる画面 |
| コード | ソースコードを提示する画面 |
| 図解 | 要素の関係を図で示す画面 |
| まとめ | 内容を締めくくる画面 |

仕様書に種別が明記されている場合は、それをそのまま採用する。明記されていない場合は、仕様書の画面要素の記述を根拠として決定する。根拠として用いてよいのは仕様書の画面要素の記述だけとし、見出しや主張の語感から推測しない。

画面要素の記述が無いシーン、または記述が複数の種別に等しく当てはまり一意に決められないシーンがある場合は、処理を停止する。対象のシーンと画面要素の記述を列挙して利用者へ提示し、どの種別で表すかの指定を待つ。

決定した種別は、根拠とした画面要素の記述とあわせて台本の提示に含める。

### 承認

台本が揃ったら、全シーンの文言と種別を利用者へ提示し、承認を得る。承認を得るまで次段へ進まない。承認を得るまでは、動画生成プロジェクトの構築・レンダリング・ファイルの書き出しを行わない。修正の指示を受けた場合は、指示された範囲だけを直してもう一度提示する。

完了条件: 全シーンの画面表示テキストと種別が確定し、利用者の承認を得ている。

## P5 規模判定

シーン数と総尺がこの手順の扱える範囲に収まっているかを判定する。

| 項目 | 上限 |
|---|---|
| シーン数 | 8 |
| 総尺 | 120 秒 |

総尺は、各シーンの想定尺の合計とする。想定尺が欠けているシーンがある場合は、P6 の尺の下限式で算出した値を用いて判定する。

いずれかの上限を超える場合は処理を停止する。先頭からの部分生成を行わず、どのシーンを対象とするかの指定を利用者へ求め、対象範囲が明示されるまで待つ。

仕様書がシーンの優先度・スキップ可否を明示している場合に限り、その記述を根拠として対象シーンを決めてよい。根拠として用いてよいのは仕様書の記述だけとする。決めた対象と、根拠とした記述を利用者へ提示してから次段へ進む。根拠を持たないままシーンを間引く判断をこの手順の側で行わない。

完了条件: シーン数と総尺がいずれも範囲内である。

## P6 構成データ生成

P4 で承認を得た台本を正規化し、シーン定義を構成データファイルとして作業ディレクトリへ書き出す。

構成データファイルは画面文言の唯一の保持先である。シーンの実装コードへ文言を直接埋め込まない。文言の修正がデータの編集のみで完結する構造とする。

承認を得た台本の文言を要約・言い換え・翻訳しない。原文のまま採用した文言も、この手順の側で起こした文言も、承認された姿のまま保持する。表記を変えたい箇所がある場合は、変更せずに完了報告で指摘する。

構成データは、シーンの配列として次の項目を持つ。配列の並びが仕様書のシーンの順序を保持する。

| 項目 | 内容 |
|---|---|
| 識別子 | シーンを一意に指す文字列 |
| 種別 | タイトル / 箇条書き / 端末 / コード / 図解 / まとめ のいずれか |
| 見出し | シーンの主題 |
| 画面表示テキスト | 承認を得た台本の文言 |
| 区分 | 逐語 / 要点 のいずれか |
| 尺 | 秒数。下限式を満たす値 |

### 尺の下限

各シーンの尺には下限がある。下限は、そのシーンの画面表示テキストの総文字数を毎秒6文字で除した値に、表示前後の余白2秒を加えた値とする。

```text
尺の下限（秒） = 画面表示テキストの総文字数 ÷ 6 + 2
```

仕様書の指定尺が下限を下回る場合は、下限値を採用する。採用した値と仕様書の指定値の差分を完了報告に載せる。

想定尺のみが欠けている場合は、下限式で算出した値を利用者へ提示する。承認を得たうえで処理を再開する。承認を得ずに算出値を確定しない。

下限値を採用した結果として尺が増えるため、採用後の値で総尺を計算し直し、P5 の上限に収まることを確認する。上限を超えた場合は P5 の停止手順に従う。

完了条件: 承認を得た台本の文言がそのまま保持され、各シーンの尺が下限式を満たしている。

## P7 環境準備

作業ディレクトリに動画生成プロジェクトを構築する。フレーム単位で描画結果を確定でき、CLI から mp4 を書き出せる動画生成フレームワークを用いる。

プロジェクトは、P6 の構成データファイルを読み込んでシーンを組み立てる構造とする。構成データを実装へ写し取らない。合成の設定は出力仕様に合わせ、解像度 1920x1080、フレームレート 30fps とする。

プロジェクトを起動したら、日本語フォントの可用性を判定する。構成データの画面表示テキストのうち、漢字・ひらがな・カタカナを含むものを1件選び、そのシーンのフレームを静止画として書き出す。書き出した静止画を確認し、日本語グリフの欠落（豆腐状の矩形・記号への置換・文字の消失）が無いことを判定する。

日本語グリフが欠落している場合は処理を停止し、必要なフォントの導入を利用者へ求める。フォントの自動導入を行わない。代替フォントへの暗黙の切り替えを行わない。判定を省略して次段へ進まない。

用いるフォントは、商用利用可のライセンスであることを確認してから採用する。ライセンスを確認できないフォントを採用しない。採用したフォント名とライセンスの確認結果を完了報告に載せる。

完了条件: プロジェクトが起動し、書き出した静止画で日本語グリフが欠落しないことを確認できている。

## P8 シーン実装とレンダリング

### シーン実装

構成データの各シーンを描画するシーンを実装する。画面文言は構成データから読み込む。実装コードへ文言を直接書かない。

表現は次の要件を満たす。

- 実写・画面録画・写真素材を用いない。画面表現は2Dの描画で構成する。この手順は撮影工程を持たない。
- 端末画面は黒背景・等幅フォント・カーソル表現で表す。実物の忠実な再現を目的としない。配色・枠線・タブは読みやすさを優先して設計する。
- 正確さを要求する対象は、視聴者が入力するコマンド文字列と、対象が出力する文言の要点に限る。それ以外の装飾的な画面要素に実物との一致を求めない。

アニメーションは次の要件を満たす。

- すべての時間変化する値を、現在フレーム番号を引数とする純粋関数として実装する。
- CSS のトランジション、ユーティリティクラスによるアニメーション、タイマーによる遅延実行を用いない。これらはレンダリング時に反映されないためである。
- シーン間の遷移は遷移用の連結構造を用いる。各シーンの開始フレームの手計算を行わない。
- 装飾のみを目的とした動きを追加しない。動きは、内容の理解を助ける場合にのみ用いる。

### レンダリング

シーンの実装が揃ったら、CLI でレンダリングして mp4 を書き出す。書き出す mp4 は次の仕様を満たす。

| 項目 | 値 |
|---|---|
| 形式 | mp4（H.264） |
| 解像度 | 1920x1080 |
| フレームレート | 30fps |
| 音声 | なし |
| 出力先 | 仕様書が指定する出力先パス |

出力先に同名のファイルが存在する場合は上書きしない。連番を付与した別名で出力し、既存ファイルの所在を完了報告に含める。既存ファイルの移動・改名を行わない。

### 範囲外

次はこの手順の範囲外とする。仕様書に依頼が含まれていた場合は、範囲外である旨を伝え、mp4 の生成のみを行う。

- ナレーションの生成、字幕ファイルの出力
- 3D 表現、外部レンダラによる素材の書き出し
- BGM・効果音の付与
- サムネイル画像の生成
- 公開プラットフォームへのアップロード

完了条件: mp4 が上記の出力仕様どおりに生成されている。

## P9 表示検証

レンダリングした mp4 から、各シーンの先頭・中間・末尾のフレームを静止画として抽出する。抽出した静止画を1件ずつ確認し、次の可読性の要件への適合を判定する。判定の根拠は抽出した静止画とし、確認せずに適合と判断しない。

| 記号 | 可読性の要件 |
|---|---|
| R1 | 本文の文字サイズが縦解像度の 3% 以上である。1920x1080 において 32px 以上とする |
| R2 | すべての文字と図形が、上下左右 5% の安全余白の内側に収まっている |
| R3 | 2倍速再生においても文字が判読できる大きさである |
| R4 | グレースケール表示に変換しても意味が失われない配色である。色のみで区別する表現を持たない |
| R5 | 音声を再生できない環境での視聴を前提とし、画面のみで内容が成立している |

不適合があった場合は、次の対応に従って該当段へ差し戻す。差し戻した段の完了条件を満たしてから P9 をやり直す。

| 不適合の内容 | 差し戻し先 |
|---|---|
| R1・R2・R3・R4 の不適合（判読性、安全余白からのはみ出し、配色） | P8 |
| R5 の不適合のうち、尺が不足していて読み切れないもの | P6 |

完了条件: R1 から R5 までの全要件を満たしている。

## 完了報告

P9 まで完了したら、次の5項目を報告する。

| 項目 | 内容 |
|---|---|
| 出力パス | 生成した mp4 の配置先。連番を付与した場合は、既存ファイルの所在もあわせて示す |
| シーン一覧と各シーンの種別・尺 | シーンの順序・見出し・決定した種別・採用した尺。仕様書に種別の記載が無く画面要素の記述から決定した場合は、根拠とした記述もあわせて示す。下限値を採用したシーンは、仕様書の指定値との差分も示す |
| 総尺 | 各シーンの尺の合計 |
| 採用した既定値 | 指定がなく既定値を採用した任意項目。採用したフォント名とライセンスの確認結果を含める |
| 要件の充足状況 | R1 から R5 までの各要件に対する判定結果 |

原文のまま採用したが表記に疑問が残る箇所があれば、この報告に添えて指摘する。修正はしない。

## 停止時の提示文

停止する分岐では、次の文面を用いて利用者へ提示し、応答を待つ。丸括弧の箇所は実際の値に置き換える。

必須項目が欠けている場合。

```text
動画仕様書を拝見しましたが、次の項目が見当たりませんでした。

- （欠落している項目名）

これらは動画の生成に必要ですので、内容をお知らせください。こちらで内容を補って進めることはいたしません。
```

正確な文字列の出所を確認できない場合。

```text
次の内容は画面に正確な文字列として表示する必要がありますが、出所を確認できませんでした。

- （対象の名称）

正しい値をお知らせいただくか、確認できる資料をお示しください。こちらで推測して補うことはいたしません。
```

シーンの種別を決められない場合。

```text
次のシーンについて、画面をどの見せ方で表すかを決められませんでした。

- （シーンの見出し）: （仕様書に書かれている画面要素の記述）

タイトル / 箇条書き / 端末 / コード / 図解 / まとめ のいずれで表すかをお知らせください。こちらで推測して決めることはいたしません。
```

台本の承認を求める場合。

```text
仕様書の内容から、各シーンの画面に表示する文言を起こしました。台本は次のとおりです。

- （シーンの見出し）（種別）: （画面に表示する文言）

この内容で進めてよろしいでしょうか。ご承認をいただいてから、動画の制作に入ります。
```

規模が上限を超えている場合。

```text
いただいた仕様書は、シーン数が（実際のシーン数）、総尺が（実際の総尺）秒です。
一度に生成できるのは、シーン数が8まで、総尺が120秒までです。

どのシーンを対象にするかをお知らせください。先頭から一部だけを生成することはいたしません。
```

日本語フォントが不足している場合。

```text
動画を生成する環境に、日本語を表示できるフォントが見つかりませんでした。
このまま進めますと、画面の文字が空白や記号に置き換わってしまいます。

日本語フォントの導入をお願いします。導入後に、あらためて生成を再開します。
```

想定尺が欠けている場合。

```text
次のシーンに想定尺の記載が見当たりませんでしたので、画面に表示する文字数から必要な長さを算出しました。

- （シーンの見出し）: （算出した尺）秒

この長さで進めてよろしいでしょうか。ご承認をいただいてから続きを進めます。
```

## 禁止事項

全段を通じて次を行わない。

- 実在の個人を特定できる情報（氏名・メールアドレス・アカウント名・端末上のパス）を画面表現の中に表示しない。仕様書にこれらが含まれている場合は、伏せ字化してよいかを利用者に確認してから進める。確認せずに伏せ字化しない。
- 第三者の著作物を無断で用いない。用いる場合は、主従関係が保たれ、出典を明示でき、改変を行わない範囲に限る。この条件を満たせない素材は採用しない。
- 資格情報を構成データ・実装コード・完了報告のいずれにも書かない。必要な場合は環境変数として外部化し、記述してよいのは変数名のみとする。この手順の構成では資格情報を必要としない。
- 作業ディレクトリの外にあるファイルを変更しない。出力先の既存ファイルを上書き・移動・改名しない。
